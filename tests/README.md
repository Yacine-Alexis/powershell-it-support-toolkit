# Tests

Place Pester tests in this folder as the toolkit grows.

Suggested conventions:

- Match each script with a corresponding `*.Tests.ps1` file.
- Keep fixtures and sample data small and local to the test that needs them.
- Mock network, CIM, and Active Directory dependencies where possible.
